# Local config — contains JWT, do not commit
config.json

# GGUF model files (large binaries)
models/
# symlink of same
models 
*.gguf

# Node
node_modules/

# Local Docker Compose override
docker-compose.override.yml
